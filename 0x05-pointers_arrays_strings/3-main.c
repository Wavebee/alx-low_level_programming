#include "main.h"

void _puts(char *str);

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	char *str;

	str = "i do not fear computers. I fear the lack of them - Isaac Asimov";
	_puts(str);
	return (0);
}
