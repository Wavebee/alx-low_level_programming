#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	void puts_half(char *str);
       	char *str;
	
	str = "0123456789";
       	puts_half(str);
	return (0);
}
